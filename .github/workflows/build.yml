name: Build & Auto-Release

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: TicTacToeAI_Linux
            exe_name: tictactoe-ai
          - os: windows-latest
            artifact_name: TicTacToeAI_Windows
            exe_name: TicTacToeAI.exe

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv and dependencies
        run: |
          pip install uv
          uv sync

      - name: Build with PyInstaller
        run: |
          # Nota: Agregamos tictactoe_lookup.pkl a la raíz del paquete
          uv run pyinstaller --noconfirm --onefile --windowed \
            --name "${{ matrix.exe_name }}" \
            --clean \
            --add-data "src${{ runner.os == 'Windows' && ';' || ':' }}src" \
            --add-data "tictactoe_lookup.pkl${{ runner.os == 'Windows' && ';' || ':' }}." \
            src/main.py

      - name: Create Debian Package
        if: matrix.os == 'ubuntu-latest'
        run: |
          mkdir -p package/usr/bin
          mkdir -p package/DEBIAN
          cp dist/tictactoe-ai package/usr/bin/

          # Crear archivo de control para el .deb
          cat <<EOF > package/DEBIAN/control
          Package: tictactoe-ai
          Version: 1.0.0
          Section: utils
          Priority: optional
          Architecture: amd64
          Maintainer: Victor <tu@email.com>
          Description: TicTacToe AI con Q-Learning
          EOF

          dpkg-deb --build package tictactoe-ai.deb

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            dist/${{ matrix.exe_name }}
            *.deb

  update_release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: TicTacToeAI_*
          merge-multiple: true

      - name: Update Nightly Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: "Development Build (Nightly)"
          files: |
            TicTacToeAI.exe
            tictactoe-ai
            tictactoe-ai.deb
          prerelease: true
          make_latest: true
